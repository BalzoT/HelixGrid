{
  "name": "ui",
  "projectType": "library",
  "root": "libs/ui",
  "sourceRoot": "libs/ui/src",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": [
          "libs/ui/src/**/*.{ts,tsx}"
        ]
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo \"No tests for ui yet\""
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/ui/tsconfig.json --noEmit"
      }
    }
  },
  "tags": []
}
